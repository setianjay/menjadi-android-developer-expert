<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.setianjay.watchme.search">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_search">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity
            android:name=".presentation.SearchActivity"
            android:parentActivityName="com.setianjay.watchme.presentation.home.HomeActivity"
            android:theme="@style/Theme.WatchMe.NoActionBar" />
    </application>

</manifest>